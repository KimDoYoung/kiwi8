## 프롬프트

### 마켓 선택 

1. 현재 한국은 2개의 시장 KRX와 NXT 2개가 있음.
2. 주식의 현재가를 구하기 위해서는 지금 현재 시각을 기준으로 2개의 시장 중 1개를 선택해야함.
3. 장 오픈 데이 즉 장이 열리는 날인지 판단.
4. 열리는 날이라면 시각이 09:00-15:30 사이 인지 판단. 그 시각이라면 KRX
5. 나머지는 NXT에서 현재가를 가져와야함.
```
즉 
market_choice_for_price() # 가격정보를 가져오기 위해서 시각을 기준으로 KRX,NXT 선택
market_choice_for_trade() # 매매를 위해서 시각을 기준으로 KRX,NXT, NULL인 경우 매매 시간이 아님
```

6. 현재날짜(현재시각)이 장이 오픈날인지는 판단하는 로직
    - cache_manager에서 찾아본다. get('OPENDAY','yyyymmdd')  'Y' 인 경우 현재 시각을 기준으로 선택한다.
    - 'N' 인경우 NXT
    - 값이 없는 경우
        - 토,일요일이면 'N' 으로 처리, cache_manger에 추가한다.
        - 
7. OpenTimeChecker에 2개의 함수 를 넣으면 어떨까?    

#### NXT

1. pre market : 08:00-08:50
2. main market : 09:00:30 - 15:20
3. after market : 15:40 - 20:00


### 추세 component

- prev_price_cache.py 파일을 보면 종목의 추세를 나타내는데.
- TrendBadge라는 이름으로 TREND 문자열을 의미에 맞게 넣어서 똑같은 길이의 뱃지로 만들어서 표현하면 됨.
- 오름은 red 계열, 하락은 blue계열
- 5일연속은 3일연속보다 약간 더 진한 배경색
- 등락중은 green 계열
- 데이터 부족은 회색 계열



## StkInfoProvider

- infrahub 폴더 안에 StkInfoProvider 클래스를 만듭시다. 파일명은 stk_info_provider.py
- 특정 stk_cd에 대한 즉 어떤 종목에 대한 정보를 제공하는 클래스임
- Sigleton으로 작성하고 stk_info 테이블를 주로 조회하여 정보를 취득
- 일단 함수 하나 작성 get_nxt_yn('005930') -> 'Y', 'N' 으로 리턴
- select nxt_enable from stk_info where stk_cd = '005930' 이렇게 해서 리턴
- 만약 없으면 kisapi CTPF1002R PRDT_TYPE_CD=300 PDNO=005930 로 조회 후 "NXT거래종목여부": "Y"로 응답
- 이때 stk_info에 추가해 둠. 그래야 나중에 api가 아니고 db에서 찾음




## 기간별 실현손익 현황

1. registry.ts의 1103번을 제목 '기간별 실현손익' 으로 변경한다.
2. ProfitTrendPage을 완전히 수정한다. 지금은 그냥 데모용임
3. 검색조건으로 : 기간 start_date ~ end_date, daterangepicker, 조회버튼을 둔다.
4. trend_routes.py를 새로 작성한다. v1/trend/startdate/enddate/profit 으로 작성한다.
5. 이 endpoint에서는 kiwoom: ka10074, kis: TTTC8715R, ls: FOCCQ33600 을 조사해서 결과를 받는다.
6. 즉 3개의 api를 호출한다.
7. 3개는 각각 다른 항목들을 가져온다.
8. pagedp 3개의 데이트를 각각 다른 AgGrid를 사용하여 보여준다.
9. 물론 데이터가 없으면 없다는 메세지를 보여준다.


## 나의 종목 록록

- sqls/kiwi8_ddl.sql의  my_stock 참조
- my_stock 관련 CRULD service 인 my_stock_service는 완전히 다시 작성하여야함.
- 이 테이블의 목적 및 method
    1. 보유/관심 주식의 리스트를 구한다. : get_list
        - spec을 풀어서 list<object>으로 리턴해야한다
    2. base_price, sell_rate,sell_price, buy_rate, buy_price로 자동 매매를 한다.
       각각 set,get method, rate가 set시 price는 자동계산, price가 set시 rate는 자동계산       
    3. spec은 json데이터를 갖고 있는다. 
    4. spec컬럼을 json형태로 채운다. fill_spec
        1. 키움의 ka10001 과  ka10100으로 채운다.
        2. 아래 내용을 json형태로 저장
    5. scheduler에 의한 동작
        1. 3개의 증권사 accounts 리스트를 통해서 보유한 종목의 리스트 hold_stocks를 구한다.
        2. hold_stocks를 돌면서 my_stocks에 없다면 stkcd/nm을 추가한다.
        3. my_stock의 모든 레코드를 돌면서. fill_spec을 채운다.
        3. my_stocks를 돌면서 is_holds가 true인 것들에 대해서  base_price를 갱신한다.
            - base_price가 비어 있으면 전날 종가로.(rate는 15% 가 default임)
            - 전날종가가가 더 큰 숫자이면 base_price를 update
            - 그렇치 않으면 db값 유지
            - base price가 조종됨에 따라서  sell_price, buy_price를 갱신한다.
            - sell,buy_rate가 없다면 sell,buy price는 비워둔다.
    6. 1203 화면번호를 구현합시다. MyStockPage.tsx 
``
spec

-상태(감리구분)
-상장일
-회사크기분류
-업종명
-NTX

-시가총액
-PER
-PBR
-매출랙
-영업이익

-상장주식수
-외인소진율
-유통비율

-상한가
-하한가
-기준가
``

-- ---------------------------------------------------------------
-- my_stock
-- ---------------------------------------------------------------
DROP TABLE IF EXISTS my_stock;
CREATE TABLE IF NOT EXISTS my_stock (
  stk_cd     TEXT    NOT NULL,                               -- 종목코드 (PK)
  stk_nm     TEXT    NOT NULL,                               -- 종목명
  sector     TEXT    NULL,                                   -- 분야
  is_hold    INTEGER NOT NULL DEFAULT 0                      -- 보유여부 (0/1)
             CHECK (is_hold IN (0,1)),
  is_watch   INTEGER NOT NULL DEFAULT 0                      -- 관심여부 (0/1)
             CHECK (is_watch IN (0,1)),
  base_price  INTEGER NULL,                                   -- 기준가(증가시 update)
  sell_rate    REAL    NULL,                                   -- 매도가격 결정을 위한 비율
  sell_price   INTEGER NULL,                                   -- 매도목표가
  buy_rate     REAL    NULL,                                   -- 매수가격 결정을 위한 비율
  buy_price    INTEGER NULL,                                   -- 매수목표가           
  spec       TEXT    NULL,                                   -- 여러가지 정보 JSON 직렬화 (예: {"매수목표가": "100000", "손절가": "90000"})
  note       TEXT,                                           -- 메모
  created_at TEXT    NOT NULL DEFAULT (strftime('%Y-%m-%d %H:%M:%S','now','localtime')),
  updated_at TEXT    NOT NULL DEFAULT (strftime('%Y-%m-%d %H:%M:%S','now','localtime')),
  PRIMARY KEY (stk_cd)
);